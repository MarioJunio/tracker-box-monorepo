name: micro_app_track_creates
description: A new Flutter module project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  commons_dependencies:
    path: '../commons_dependencies'
  commons_shared:
    path: '../commons_shared'
  commons_design_system:
    path: '../commons_design_system'
  micro_app_map_tracks:
    path: '../micro_app_map_tracks'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mobx_codegen: ^2.0.7
  build_runner: ^2.1.11

flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: com.example.micro_app_track_creates
    iosBundleIdentifier: com.example.microAppTrackCreates
